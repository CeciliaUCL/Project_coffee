import 'package:flutter/material.dart';

class MyDevicePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Device Page'),
    );
  }
}
